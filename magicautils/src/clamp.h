#ifndef CLAMP_H
#define CLAMP_H
/*
Util functions for clamping values
*/

int clamp(int x, int min, int max);
float clampf(float x, float min, float max);
int min(int a, int b);
int max(int a, int b);
float minf(float a, float b);
float maxf(float a, float b);

#endif // CLAMP_H
